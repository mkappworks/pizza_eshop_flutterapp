name: pizza_eshop_flutterapp
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.0
  flutter_svg: ^0.17.4
  smooth_star_rating: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/